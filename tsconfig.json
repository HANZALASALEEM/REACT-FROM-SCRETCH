{
  "compilerOptions": {
    "target": "ESNext", //? Transpile typescript if low version its compatible with even older browsers
    "allowJs": true, //? Also check JS files (No need in this project)
    "esModuleInterop": true, //? Import from CommonJS module
    "allowSyntheticDefaultImports": true, //?Allows default imports from modules that may not have a default export
    "strict": true, //? Strict type checking
    "forceConsistentCasingInFileNames": true, //? Ensures that file names are consistently cased (cause ambiguity during import)
    "module": "esnext", //? Useful for webpack
    "moduleResolution": "node", //? Necessary for importing files (specially from node_module)
    "resolveJsonModule": true, //? Allows importing JSON files (No need in this projrct)
    "isolatedModules": true, //? Transpiled each file independently (useful with webpack)
    "jsx": "react-jsx" //? For using JSX no need to explicitly import React in every file (useful for React 17+)
  },
  "include": ["src"]
}
